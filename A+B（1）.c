#include <stdio.h>
int main()
  {double A, B, sum;
   A <= fabs ( 10 * 15 );
   B <= fabs ( 10 * 15 );
   scanf("%lf %lf",&A,&B);
   sum = A + B;
   if ( sum - (int)sum == 0) {
       printf("%.lf",sum);
   } else {
        printf("%.2lf",sum);
   }
   return 0;
  }
